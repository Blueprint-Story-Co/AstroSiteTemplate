---
import Layout from "./BaseLayout.astro";

const {title} = Astro.props;
---

<Layout title={title}>
    <section class="hero">
        <h1>{title}</h1>
    </section>
    <section class="content">
        <slot />
    </section>
    
</Layout>